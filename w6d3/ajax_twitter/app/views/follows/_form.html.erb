<% if current_user.follows?(user) %>
  <% status = "followed" %>
<% else %>
  <% status = "unfollowed" %>
<% end %>

<button
  type="button"
  class="follow-toggle"
  data-user-id="<%= user.id %>"
  data-initial-follow-state="<%= status %>"></button>
